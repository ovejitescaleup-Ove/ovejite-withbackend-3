# GitHub upload

This folder is the complete project root. Upload the CONTENTS of this folder to the repository root — not this ZIP file and not the parent folder.

Required root files include `package.json`, `next.config.mjs`, `app/`, `src/`, `public/`, and `base44/`.
